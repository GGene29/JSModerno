//ACEDIENDO A LOS VALORES DE UN OBJETO

const ramas = {
    prueba: "primer merge desde pruebas",
    hoja: "su color es verde",
    cantidad: 5,
};
console.log(`Este es un objeto ${ramas}`);

console.log(`Aqui accedemos al valor especifico ${ramas.prueba}`);

console.log(`Esta es otra manera de acceder a valores especificos ${ramas['hoja']}`);