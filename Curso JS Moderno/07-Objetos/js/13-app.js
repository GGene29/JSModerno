const final = {
    paso1:"Cantar",
    paso2:"Bailar",
    paso3:"Hola"
}
//Se puede usar para el obtene tiene o no tiene enformación. 
console.log(Object.keys(final));
//Retorna por lo que esta compuesto el objeto o su parte izquierda



console.log(Object.values(final))
//Retorna los valores del objetos

console.log(Object.entries(final));
//Retorna todo en partes, pero retorna todo